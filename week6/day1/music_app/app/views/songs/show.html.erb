<p id="notice"><%= notice %></p>

<h1>
  <strong>Title:</strong>
  <%= @song.title %>
</h1>

<%= link_to image_tag(@song.cover_image.thumb.url), @song.cover_image.detail.url %>

<br>

<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Back', songs_path %>
