<h1>Add a Song</h1>

<%= form_for @song do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.collection_select(:artist_id, Artist.all, :id, :name) %>

	<%= f.label :genre %>
	<%= f.text_field :genre %>

	<%= f.submit %>

<% end %>
